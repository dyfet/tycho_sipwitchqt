#include <stdio.h>

int main(int argc, char const *argv[]) {
  printf("Hello World we use Docker for CI/CD, trying kubernetees!\n");
  printf("Lets see whether the CI will work with merge.\n");
  return 0;
}
